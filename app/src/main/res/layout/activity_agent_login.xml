<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:tools="http://schemas.android.com/tools"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:paddingBottom="@dimen/activity_vertical_margin"
              android:paddingLeft="@dimen/activity_horizontal_margin"
              android:paddingRight="@dimen/activity_horizontal_margin"
              android:paddingTop="@dimen/activity_vertical_margin"
              tools:context="io.fenogy.clouddialer.AgentLogin"
              android:orientation="vertical"
              android:gravity="center">

    <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Agent Login"
            android:textSize="30dp"
            android:gravity="center"
            android:layout_marginBottom="20dp"/>

    <EditText
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/agent_username"
            android:inputType="text"
            android:maxLines="1"
            android:background="@drawable/rounded_rect_bkg"
            android:hint="enter username"
            android:layout_marginBottom="10dp" android:padding="10dp"/>

    <EditText
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/agent_password"
            android:inputType="textPassword"
            android:maxLines="1"
            android:background="@drawable/rounded_rect_bkg"
            android:hint="enter password"
            android:layout_marginBottom="20dp" android:padding="10dp"/>

    <ImageButton
            android:layout_width="180dp"
            android:layout_height="60dp"
            android:text="Login"
            android:id="@+id/agent_login_button"
            android:layout_marginBottom="10dp" android:background="@drawable/login_button"/>

    <ImageButton
            android:layout_width="180dp"
            android:layout_height="60dp"
            android:text="Click here to register"
            android:textSize="20dp"
            android:gravity="center"
            android:id="@+id/agent_register" android:background="@drawable/register_button"/>

</LinearLayout>